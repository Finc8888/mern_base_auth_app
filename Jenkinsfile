node {
    // v2
	checkout scm

	stage('Build Base Auth application') {
		echo "*** Build Base Auth application ***"
        pwd
	}

	stage('Deploy Base Auth application'){
		echo "*** Deploy Base Auth application ***"
        ll
	}
}